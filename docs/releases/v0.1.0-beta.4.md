# v0.1.0-beta.4 release candidate

Status: **Prepared in source; not published.** This document is a candidate
plan, not a registry availability or release-qualification receipt.

Branch: `rc/0.1.0-beta.4`, to be created from the reviewed `main` commit that
merges this candidate preparation. The previous source tag is
`v0.1.0-beta.3` at `848b179aee32789a6f8b0ad4552a3a1262a05d55`.

## Product acceptance

This candidate follows product acceptance of
[#137](https://github.com/agent-topology/agent-topology/issues/137) ("Make
expanded topology usable for internal consumers"), which coordinated #136,
#139, #140, #141, and #142 and closed with every success-evidence checkbox
satisfied: an accepted nesting/addressing/compatibility ADR
([ADR 0012](../decisions/0012-nested-graph-identity-traversal-and-compatibility.md)),
shared Python/TypeScript conformance at depths 0/1/2, API-equivalent CLI
depth, a documented minimal factory-export/graph-ID integration, and
committed internal-consumer evidence with an IC-01–06 disposition — see the
[internal-consumer research index](../research/internal-consumer/README.md#final-dispositions-issue-154).
This document prepares the coordinated candidate that evidence unblocks; it
does not itself qualify or publish artifacts — see
[#144](https://github.com/agent-topology/agent-topology/issues/144) and
[#145](https://github.com/agent-topology/agent-topology/issues/145).

## Selected packages

| Package | Candidate version | Runtime | Framework |
| --- | --- | --- | --- |
| `agent-topology-spec` | `0.1.0b4` | Python 3.11–3.14 | None |
| `agent-topology-langgraph` | `0.1.0b4` | Python 3.11–3.14 | LangGraph 1.2.10–1.2.11 |
| `@agent-topology/spec` | `0.1.0-beta.4` | Node.js 20+ | None |
| `@agent-topology/langgraph` | `0.1.0-beta.4` | Node.js 20+ | LangGraph.js 1.4.14 |

The Python producer requires `agent-topology-spec>=0.1.0b4,<0.2.0`.
The npm producer's specification peer is `@agent-topology/spec@0.1.0-beta.4`.
The Python `agent-topology-langgraph` distribution alone owns `agt describe`.
The document format remains `0.1` and the structure-hash algorithm version
remains `1`. The four package versions were selected together for this
candidate; future releases remain independent.

**Published installation commands are unchanged.** README, per-package
READMEs, and the quickstarts continue to select the verified beta.3 versions
(`0.1.0b3` / `0.1.0-beta.3`) until beta.4 passes public registry verification.
This document and the [upgrade guide](../guides/upgrading-beta.4.md) are the
labeled candidate instructions.

## Changes from beta.3

- Both LangGraph producers retain a confirmed compiled child's parent node
  under its own id at every depth instead of flattening it away. Within the
  requested `depth` budget, the parent node additionally gains a core
  `subgraphId` addressing the child as its own first-class `graphs[]` entry,
  deterministically derived as `parentGraphId:parentNodeId`; a derived id that
  would collide with an existing graph id is never emitted, and the node
  stays opaque with a new `child-graph-id-collision` gap instead. `depth = N`
  now materializes levels `1..N`; level `N`'s own children keep the unchanged
  depth-0 opaque-child contract, and depth-0 output remains byte-identical to
  today's. `expanded-subgraph-metadata` is retired: it can no longer be
  emitted. See
  [ADR 0012](../decisions/0012-nested-graph-identity-traversal-and-compatibility.md).
- Both LangGraph producers now populate `branch`, `sentinel`, and `entry`
  experimental interpretation facts for a materialized child's own nodes,
  using the same [ADR 0008](../decisions/0008-experimental-consumer-interpretation.md)
  evidence rules already used for a root graph — a materialized child is not
  a degraded view. `x-topology-interpretation` advances to revision `"2"` on
  any graph that materializes at least one child, adding a `subgraph` value
  of `materialized-child` (evidence kind `materialized-subgraph-reference`,
  referencing the containing `graphs[].id` and the node's `subgraphId`).
  `opaque-child` is invalid on any node carrying core `subgraphId`, and
  `materialized-child` is invalid without one. Depth-0 documents and any
  document with no materialized node continue to use revision `"1"`
  unchanged. See
  [ADR 0012](../decisions/0012-nested-graph-identity-traversal-and-compatibility.md).
- The Python CLI exposes the `describe` API's traversal depth through
  `agt describe --depth N`, defaulting to `0` (backward compatible, opaque
  children) and accepting only non-negative integers; an invalid, missing, or
  negative value is a usage error (status `2`). `agt describe` output at a
  given depth is equivalent to calling the Python API with the same `depth`,
  including `--strict` status `6` for an actual graph-specific gap. The CLI
  still only imports and describes a module-level compiled-object target; it
  does not invoke the graph or call a factory to build one. See
  [ADR 0007](../decisions/0007-langgraph-owns-the-foundation-cli.md) and the
  [CLI reference](../reference/cli.md).
- A caller whose target object requires a factory (policy, model gateway, or
  other invocation-time argument) is not a supported CLI target; `agt` never
  invokes a factory or accepts factory arguments. An import-safe module that
  constructs and exports the compiled object at import time, without
  invocation-time side effects, remains the supported pattern. See the
  [factory-export recipe](../research/internal-consumer/import-safe-export/README.md)
  and the [upgrade guide](../guides/upgrading-beta.4.md#factory-owned-graphs-and-the-cli-target).
- Positive-depth address correlation (parent identity, repeated child call
  sites, and materialized-child identity) is exercised end to end against the
  current producer, and a real dynamic-interrupt/resume/repeated-attempt
  capture proves the boundary between what the document can state statically
  and what only a runtime observation can confirm. See the
  [internal-consumer research index](../research/internal-consumer/README.md#final-dispositions-issue-154)
  and the [upgrade guide](../guides/upgrading-beta.4.md#topology-vs-dynamic-pauses-approval-validity-and-runtime-effects).

See the [Unreleased changelog](../../CHANGELOG.md#unreleased) for detailed
evidence and links.

## Qualification

Qualified on the frozen source commit
[`05486d7`](https://github.com/agent-topology/agent-topology/commit/05486d7ee9cee7650779cb0e9c00a85bd152a3e8)
(the `main` merge of the candidate preparation in #143) from the
`rc/0.1.0-beta.4` branch, with every release workflow run using `publish=false`.
No artifact was published to any registry. Full receipts, workflow run links,
and the manual positive-depth artifact verification are in
[`evidence/beta.4/qualification.json`](evidence/beta.4/qualification.json);
per-package receipts are alongside it
(`evidence/beta.4/python-spec-qualification.json`,
`python-langgraph-qualification.json`, `npm-spec-qualification.json`,
`npm-langgraph-qualification.json`).

- Python and TypeScript producer/spec matrices, shared nested conformance,
  canonical/hash/reference regressions, and every supported LangGraph and
  LangGraph.js version passed:
  [Python packages](https://github.com/agent-topology/agent-topology/actions/runs/35031211369),
  [TypeScript packages](https://github.com/agent-topology/agent-topology/actions/runs/35031211286),
  [LangGraph compatibility](https://github.com/agent-topology/agent-topology/actions/runs/35031211412).
- All four artifacts were built, inspected, and clean-installed independently:
  [`agent-topology-spec`](https://github.com/agent-topology/agent-topology/actions/runs/35031234745),
  [`agent-topology-langgraph`](https://github.com/agent-topology/agent-topology/actions/runs/35031236442),
  [`@agent-topology/spec`](https://github.com/agent-topology/agent-topology/actions/runs/35031238044),
  [`@agent-topology/langgraph`](https://github.com/agent-topology/agent-topology/actions/runs/35031239906).
  Producer/spec namespace coexistence and spec-only installation both passed
  for each ecosystem.
- The new positive-depth materialization behavior — not exercised by the
  unchanged install smoke scripts — was verified directly against the
  downloaded wheel and tarball artifacts with a minimal two-level nested-graph
  fixture in both languages; see `artifactDepthVerification` in the
  qualification evidence for the exact shapes observed at depth 0 and depth 1.
- Consumer replay and cross-language regression suites with no dedicated CI
  workflow (`tests/test_consumer_regression.py`,
  `tests/test_internal_consumer_capture.py`,
  `tests/test_internal_consumer_dynamic_capture.py`,
  `tests/test_trace_correlation_example.py`, `spec/tests`) were run directly
  against the frozen commit and passed.
- The registry-backed producer/spec preflight (`verify_spec_registry.py`,
  `verify_npm_registry_peer.mjs`) is gated behind `publish=true` in both
  release workflows and was correctly not exercised here: beta.4's
  specification packages are not yet on a public registry to preflight
  against. That check belongs to the publication Task (#145), per this
  document's own product-acceptance note above.
- Every qualification workflow run confirmed a clean checkout
  (`git diff --exit-code` and no untracked changes) after building and
  installing. No artifact or source was changed after qualification; any
  future correction requires a new frozen commit and re-qualification.

## Publication checklist

- [x] Create `rc/0.1.0-beta.4` from the reviewed `main` commit that merges
      this candidate preparation; route any RC-only correction through a PR
      targeting that branch.
- [x] Pass package CI and framework compatibility checks on the frozen commit.
- [x] Qualify each package with `publish=false`; record the exact receipts
      and digests.
- [ ] Publish the Python specification, then its producer.
- [ ] Publish the npm specification, then its producer.
- [ ] Install every exact registry version in a clean environment and verify
      public APIs, including the depth-materialization and revision-2
      interpretation behavior above.
- [ ] Record artifact URLs, digests, source commit, provenance, and workflow
      receipts here.
- [ ] Create `v0.1.0-beta.4` at the qualified commit, then the GitHub
      prerelease.
- [ ] Update public installation guides from beta.3 to beta.4 only after
      verification.
- [ ] Merge release changes back into main through a PR.

No beta.4 tag or GitHub Release should imply publication before those checks
pass. Do not replace the existing beta.1, beta.2, or beta.3 registry
artifacts. Follow the
[release and partial-publication recovery guide](../releasing.md).
